# Leave empty for same-origin (Next.js + FastAPI reverse-proxied under one domain).
# Set to the full FastAPI URL if deployed on a separate origin, e.g.:
# NEXT_PUBLIC_API_BASE_URL=https://policysense-backend.onrender.com
NEXT_PUBLIC_API_BASE_URL=
